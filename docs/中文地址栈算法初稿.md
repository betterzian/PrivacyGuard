1、clue是hard也不可以直接走基类，需要正常地址解析。2、soft 解析状态机改成clue解析状态机。3、从key起栈，需要向左扩两个unit。4、从value起栈，英文需要向左找一个digit（如果存在）。5、尾部逆序不是一直逆序，需要有逗号才能逆序，如果没有逗号就需要继续按照正序继续可以多次逆序但需要建立地址类型数组，已有的层级需要占位，不可以同一层级多次出现，比如"南京中路，上海浦东"，“南京中路，浦东，上海”，也可以“南京中路，上海浦东，中国” 但不可以“南京中路，浦东上海中国”或者“南京中路，浦东上海，天元西路”。6、对于value层级的key层级或者key和key的关系如下，如果value或key和下一个key是紧接着或者只间隔一个unit，则该value和key都是后面key的value的一部分，并且有传导关系。比如“扑南京人西路”，那么西路是key，扑和南京和人组成的"扑南京人"是key的value，比如科技园社区小区，那么科技园归属社区，社区归属小区，则小区为最终的key，而他的value是科技园社区。7、一个value如果在后面找到key，那么这个value就默认是他自己的层级，比如上海巴拉巴拉，只有上海这个value，则认为这就是城市component。否则根据6进行。8、如果当前地址数组指示已经有省或市或区或者其他应当一个地址只出现一次的component，那么地址里还出现了相关的component，则判断是两个地址，从当前component起始位置将两个地址拆分。每增加一次component则在地址组件上占位。9、对于地址数组里没有出现的component，如果和他们绑定的key和他们的原始代表的层级不一样，则需要在提交component时特殊提示。比如南京浦东中山路，没有出现市和区的占位，则南京和浦东被提取为疑似信息，然后对该路名进行提交时变成{text：中山，疑似信息：city=南京，district=浦东}。比如南京科技园路则路的value是{text:科技园，疑似信息:city=南京}。比如浦东科技园路则路的value是{text:科技园，疑似信息:district=浦东}。如果出现了地址数组里出现了区的占位或者后面因为逆序而补上了区的占位，则南京和浦东都不应当认为疑似是city或district，因为不符合后继要求（除非有，）,则他们被认为是普通信息，去除他们的clue类型，此时路的value是{text：南京浦东中山}和{text:南京科技园}和{text:浦东科技园}。如果只补上了市的占位，那么只有南京不会被认为是疑似city，浦东依然是疑似district，此时路的value是{text：南京浦东中山}和{text:南京科技园}和{text:科技园，疑似信息：district=浦东}。其中南京浦东中山是因为南京已经不符合疑似信息，那么从南京往后的clue都不符合疑似信息。10、对于poi这种可能出现多个component的poi时，除了不需要满足一个类型只能一个component这个条件，其他条件都一样。在提交组件时：把他们的key全部去除，只留下value数组。举个例子：{携程阳光社区小区里的住宅楼}，会有三个poi的key，社区，小区，楼，社区和小区之间隔了不超过一个unit，则归为小区value，然后小区和楼超过1个unit，则是两个key，但是都是poi，于是poi提交为[携程阳光，里的住宅]。11、在canonical对比时，做出如下改变：numbers比较方式一样。有单一component类型比较方式一样，如果有的话。对于存在疑似信息的组件比如是A与另一个组件B对比，先比较A和B的text，看看是否是其中一个的子集，不满足则不同，满足则继续以以下顺序比较：”用疑似信息里的key比较对方疑似信息里的相同的key（如果有），否则用疑似信息里的key去比较对方的text看是否在text里面，最后用key去比较对方的对应的key层级的component的value。顺序比较，一旦有一个满足，则认为两个组件一样。最后是对于poi这种多component类型，A与B两个地址的poi数组里，只要有一个数组元素是另一个数组元素的子集，就认为是同一个地址。12、canonical比较顺序：province->city->district->road->numbers->subdistrict->poi，其中任意一个双方都有的层级校验不通过，则认为不是同一地址，并立即停止校验。



第一个例子：上海钟山路南京西小区北京->上海city value，路 road key，南京 city value，小区 poi key，北京 city value。

1、上海 -钟山-路，address数组里city及city层级以下的key占位为0，组件提交，key=road，value=上海钟山，suspect：city=上海。address数组里 road占位。

2、找到南京，疑似为city，address数组里city及city层级以下的key占位为1（road），则南京要么是逗号的逆序要么是普通文字（没有城市语义的普通信息）才合法，然后向后寻找有没有南京前面的路的key即road的合法的后继key，然后找到小区，因此南京是普通信息，然后吃下小区，上海-钟山-路-南京西-小区，小区这个poi key的value是从路后面一个取值到小前面一个字，即南京西为value。address数组里poi占位（poi后续还有要么+1要么可以不写入，这个无所谓）。

3、找到北京，疑似为city，address数组里city及city层级以下的key占位为2（road，poi），则北京要么是逗号的逆序要么是普通文字才合法，向后寻找key，找不到，则其需要standalone，北京必然是city层级的value，然后没有发现逗号，非法逆序，所以北京是非法吸入地址（不该处理）,因此从最后一个吸收的clue即小区停止，然后进行正常提交判定流程，提交“上海钟山路南京西小区“，然后调用函数对每个suspect所在component进行修改，发现上海 -钟山-路里有suspect，city=上海，则从poi=上海钟山里面删除从左到右出现的第一个上海， 最后提交的components包含{[key=road,value=钟山，suspect:{city=上海}]，[key=poi,value=[南京西，]，suspect={}]}，然后run提交的index应当是小区后一个clue的index。

第二个例子：浦东钟山路浦东西小区，浦东，上海  ->浦东 district value，路 road key，浦东 district value，小区 poi key，浦东 district value，上海 city value。

1、浦东 -钟山-路，address数组里district及district层级以下的key占位为0，组件提交，key=road，value=浦东钟山，suspect：district=浦东。address数组里 road占位。

2、找到浦东，疑似为district，address数组里district即district层级以下的key占位为1（road），则浦东要么是逗号的逆序要么是普通文字（没有区语义的普通信息）才合法，然后向后寻找有没有浦东前面的路的key即road的合法的后继key，然后找到小区，因此浦东是普通信息，然后吃下浦东，浦东-钟山-路-浦东西-小区，小区这个poi key的value是从路后面一个取值到小前面一个字，即浦东西为value。address数组里poi占位（poi后续还有要么+1要么可以不写入，这个无所谓）。

3、找到浦东，疑似为district，address数组里district即district层级以下的key占位为2（road，poi），则浦东要么是逗号的逆序要么是普通文字（没有区语义的普通信息）才合法，向后寻找有没有其他key会影响浦东，发现到","之前都找不到key（即使有value也不影响，只有key能影响value的层级），因此浦东是区信息，然后发现有逗号，逆序合法，然后吃下浦东，浦东-钟山-路-浦东西-小区，浦东，在address数组里的district置为，然后调用函数发现之前一个component里的suspect有district=浦东，则清除该suspect，认为他是普通信息。

4、找到上海，疑似为city，address数组里city即city层级以下的key占位为3（district, road，poi），则上海要么是逗号的逆序要么是普通文字（没有市语义的普通信息）才合法，向后寻找有没有其他key会影响上海的层级，找不到key，因此上海是city，然后发现有前方逗号，逆序合法，然后吃下上海，浦东-钟山-路-浦东西-小区，浦东，上海，在address数组里的city占位。

5、到了末尾，自然结束，进行提交前和提交处理，调用函数处理suspect，发现没有component里有suspect，则直接提交，因此最后的提交component是{[key=road,value=浦东钟山]，[key=poi,value=[南京西，]，suspect={}],[key=district,value=浦东],[key=city,value=上海]}。

第三个例子：上海市浦东西康路，上海city value，市city key ，浦东district value, 路 road key。

1、上海，address数组里city及city以下没有key，因此上海疑似city，然后向后找，找到市，上海市，value->key，key后面没有紧跟着另一个key，因此是一个组件，上海市，component是key=city，value=上海，suspect={}。address数组里。

2、向后找找到浦东，address数组里district以及district以下没有key，因此浦东疑似district，然后向后找找到路，路后没有其他key，因此组成组件，浦东西康路，提交为component{key=road, value=浦东西康，suspect={district=浦东}}。address里road占位。

3、到了末尾，自然结束，进行提交前和提交处理，调用函数处理suspect，发现road组件里suspect有district=浦东，则删除value里从左向右第一个出现的浦东，然后最终提交地址，其component是{[key=city,value=上海]，[key=road,value=西康，suspect={district=浦东}]}。

